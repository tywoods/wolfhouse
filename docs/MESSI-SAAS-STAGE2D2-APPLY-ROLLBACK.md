# MESSI SaaS Stage 2D2 — temporary apply / rollback / expiry-status

Executable owner for a **temporary** `luna-<slug>-staging-rg` drill. **Apply** plan authority is **only** Stage 2D1 `deriveAuthority` at current clean master HEAD (exact-SHA snapshot, pinned tools, staging subscription, compiled Bicep bytes). **Rollback** may use Stage 2D1 `deriveHistoricalRollbackAuthority` from immutable live RG tags when `deploySha` is a prior master ancestor (full 40-hex commit, ancestor of HEAD and `origin/master`, not side-branch-only): snapshot that exact SHA, rederive names/contract/`planDigest`, require rederived digest equals live `planDigest` and full drill tuple match. Receipt is never authority. Callers supply slug + approval flags — never manifest/names/sub/RG/state.

**Lifecycle mode (fail-closed):** Default **`temporary-drill`** is byte-stable (TTL/cost-cap/`temporaryDrill` unchanged). Internal **`durable-staging`** reuses this apply path (not a parallel owner/CLI): binds mode into planDigest/tags/receipt/authority; success preserves RG/runtime (no temp/TTL/destroy-after-success); failed partial uses canonical rollback (`rollbackOnFailure` → `_internalFailureRollback`). Public CLI lifecycle flags refuse zero-mutation until a later readiness/approval boundary; activate only via `_internalDurableLifecycle`.

**Local runtime preflight (before ACR build / RG create-adopt / role PUT / any ARM write):** Apply loads the C2 operator only after Azure foundation (`loadC2Operator` → `scripts/bootstrap-synthetic-tenant-db.js` → `scripts/run-canonical-migrations.js` → `pg`). A clean checkout without `node_modules` previously created the foundation then failed with `Cannot find module 'pg'`. Apply now requires that exact call-graph dependency to resolve **inside this worktree's own `node_modules` install** (not ambient `NODE_PATH`, ancestor `node_modules`, or out-of-worktree linked packages) and those entrypoints **before** ACR build, RG mutation, role creation, or any ARM PUT/DELETE; fail-closed with stable `local_runtime_dependency_missing` / `local_runtime_entrypoint_missing` (no auto-install; sanitized errors — no abs Require stacks, env, credentials, or DSNs). The frozen module/entrypoint list is **manually maintained** from the known C2 call graph and is **not auto-complete** under future C2 dependency drift. Operator prerequisite (run once in the repo root; never from the safety-critical runner):

```bash
npm ci
```

**Provider preflight (before any RG mutation):** Observed for our current App Insights path (not a global unavoidability claim): App Insights creation triggered Azure platform `Failure-Anomalies-Alert-Rule-Deployment-<8hex>` and the live drill failed MissingSubscriptionRegistration when the fixed staging subscription lacked `Microsoft.AlertsManagement` Registered; no supported disable property exists in our template/repo. Apply performs the same read-only provider GET as Stage 2D1 PLAN and refuses closed (no RG PUT/adopt retag, no self-register, no subscription Contributor/Owner) unless `registrationState==Registered` and `body.id` is case-insensitively exact `/subscriptions/<exact-sub>/providers/Microsoft.AlertsManagement`. `prepare-spec` remains read-only but prints the explicit Azure-admin `az provider register --namespace Microsoft.AlertsManagement --subscription <exact-sub> --wait` plus readback first among `azureAdminCommands`.

**AcrPull (JS-owned, not Bicep):** Live drill failed when nested `*StagingAcrPull` under the tenant RG resolved `whstagingacr` in that RG (`ResourceNotFound`). Prior shared-RG nested deploy required `Microsoft.Resources/deployments/write` on `wh-staging-rg` (executor lacks it; do **not** grant subscription/shared-RG Contributor). Tenant-staging Bicep no longer emits AcrPull. After infra creates the tenant managed identity, apply PUTs the exact deterministic AcrPull (`azureArmGuid(acrId, principalId, AcrPull)`) at the existing shared ACR scope via ARM `roleAssignments` (temp ACR RBAC Admin only), requires exact principal/type/role/scope readback with bounded propagation, records receipt diagnostics, and **fail-closes before bootstrap/runtime**. Apply-failure cleanup may delete that exact AcrPull when identity is readable.

**RBAC propagation (ARM deploy boundary only):** Live prepared-RG drill (`e083a457`) had exact Contributor/RBAC then nested `privateNetwork` terminal-`AuthorizationFailed` on `virtualNetworkLinks/write`. Retry only `PUT Microsoft.Resources/deployments` when body is `provisioningState=Failed` + outer `error.code=DeploymentFailed` + nested `AuthorizationFailed` with exact quoted executor OID, `*/write` action, and `over scope` = prepared RG or `/` descendant (reject substring lookalikes, root/direct authz, bad quotes). Shared phase deadline+TTL; max 3; truthful `attempts`/`attemptErrors`; no broader perms/unrelated-403/rollback weaken.

**Bootstrap Job name (JS-owned, Bicep param):** Live messiproof drill failed bootstrap with `ContainerAppInvalidName` — canonical `luna-messiproof-staging-bootstrap` is 33 chars (Azure Container Apps Job max 32) after infra + direct AcrPull succeeded. Single owner is Stage 2D1 `deriveBootstrapJobName` (same family as `deriveKeyVaultName`): preserve `luna-<slug>-staging-bootstrap` when already valid (2..32, lowercase alnum/hyphen); overlength only → deterministic `luna-<head>-<8hex>` where the hex is sha256 of the full canonical name (collision-resistant; never raw truncation alone). Contract, Bicep `bootstrapJobName` param, D2 apply params, operator `derivedBootstrapJobNames`, fixtures, and status residual IDs all bind that owner.

**Platform-supplemental smart detector:** When AlertsManagement is Registered, Azure may create exactly one `microsoft.alertsmanagement/smartDetectorAlertRules` named `Failure Anomalies - <appInsightsName>` under the owned RG. Inventory never admits a sparse generic `/resources` row alone: every smart-detector candidate gets a pinned provider GET (`api-version=2019-03-01`) and status/rollback phase authority use the enriched GET body. Admit only status 2xx with exact name/type/id/sub/RG, `location=global`, empty tags when returned, required `properties` object, exact singleton `scope` matching the App Insights id (case-insensitive), `detector.id=FailureAnomaliesDetector` + `detector.name='Failure Anomalies'` + `supportedResourceTypes=['ApplicationInsights']` only, and `frequency=PT1M` / `severity=Sev3` / `state=Enabled`. Do **not** require prose `description` or `actionGroups` (platform text/default AG may vary). Fail closed on 403/404/500/malformed/missing props/extra scope/duplicate/lookalikes. The type is **not** broadly ignored.

Approval is a hard total cap (not monthly): `--approve-max-total-usd 8 --ttl-hours 48`. TTL 1..48h; cap ≤ $8; monthly flags rejected. Drill tags (`createdAt`/`expiresAt`/`temporaryDrill`/`planDigest`/`deploySha`/tenant/stage/owner) flow via Bicep params (empty defaults preserve Sunset). ARM/job/RG polls use wall-clock `PHASE_MAX_MS` deadlines (RG delete ≥30m), 5s backoff capped 15s, honor numeric/date `Retry-After`, check abort+expiry each iteration, keep terminal body. Phase starts and C2 start/wait/delete (incl. every `waitTerminal` poll) call `assertActiveDrill`. SIGINT/SIGTERM abort; if RG was read present, write nonsecret `rollback_failed`/`rollback_aborted` receipt; lock always `finally`. **Rollback order:** clean master `HEAD==origin/master` → resolve exact slug RG under fixed staging sub → probe live full drill tag tuple → exclusive lock → re-read RG requiring same ETag + full tag tuple (no TOCTOU) → historical authority from live tags → inventory/phase gates → **whole owned RG delete** → **exact tenant AcrPull delete** (shared ACR; does not vanish with RG; only after principalId identity from inventory; 404 already-absent OK; no list-delete) → **exact temp ACR RBAC-admin delete only**. Fail-closes on any D1 inventory finding; only exact `empty`/`infra-partial`/`foundation`/`bootstrap-active`/`runtime-prereqs`/`runtime` after full `assertDrillTags` (`empty` = zero-resource, zero-deployment owned drill only; `infra-partial` = exact owned subset of rederived foundation/nested contract with every resource ID present and equal expected, plus deployment rows with exact id/name/type/safe state under this subscription/RG path — plan-owned names and the narrow Azure-generated `Failure-Anomalies-Alert-Rule-Deployment-<8hex>` only via exact platform SHOW signature (`Failed`, pinned `templateHash`, singleton `Microsoft.AlertsManagement`/`smartDetectorAlertRules`/`global`, top error `DeploymentFailed` with exactly one `details[]` row `MissingSubscriptionRegistration` + `target` absent-or-null (CLI materializes null; ARM REST omits null keys; non-null/empty/forged refuse) and no nested detail children) after GET readback, with the expected App Insights component present in live inventory; **missing expected AcrPull is permissible as an absent member of that exact owned infra-partial subset and of foundation residual inventory** (JS-owned; failed nested `*StagingAcrPull` never created it — not unexpected/foreign) — other missing roles (e.g. KV), unexpected/wrong roles remain refused; exact platform-supplemental Failure Anomalies smart detector resource may appear). Refuse zero mutation for nonancestor SHA, missing object, digest mismatch, tag drift, wrong sub/RG/tenant, dirty/non-master checkout, or snapshot/tar/compiler mismatch. Apply never accepts historical authority. Stale op.lock recovers only after recorded PID is proven dead and no competing process (never steal live lock). C2 uses pinned `/opt/data/.local/bin/az`. Success receipt requires exact live RG tag tuple. Offline verify only — no live Azure writes.

```bash
node scripts/messi-saas-stage2d2-apply-rollback.js apply \
  --slug messiproof \
  --approve-max-total-usd 8 \
  --ttl-hours 48

node scripts/messi-saas-stage2d2-apply-rollback.js expiry-status --slug messiproof

node scripts/messi-saas-stage2d2-apply-rollback.js rollback \
  --slug messiproof \
  --confirm-delete luna-messiproof-staging-rg

npm run verify:messi-saas-stage2d2-apply-rollback
```

No background expiry daemon — `expiry-status` prints the paste-ready rollback command.
