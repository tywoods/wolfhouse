'use strict';
const assert=require('node:assert/strict');
const fs=require('node:fs');
const path=require('node:path');
const graph=require('./lib/email-microsoft-graph-messages-delta-page-transport');
const runtime=require('./lib/email-delta-sunset-staging-runtime-composition');
const root=path.join(__dirname,'..');
const mailbox='11111111-1111-4111-8111-111111111111';
const watermark='2026-08-14T12:00:00.000Z';
const initial=graph.buildMessagesDeltaFromNowInitialPath(mailbox,watermark);
assert.equal(initial,`/v1.0/users/${mailbox}/mailFolders/inbox/messages/delta?$top=5&$select=id,subject,from,receivedDateTime,isRead,conversationId,internetMessageId&$filter=receivedDateTime%20ge%202026-08-14T12%3A00%3A00.000Z`);
assert.equal(graph.buildMessagesDeltaFromNowInitialPath(mailbox,'bad'),null);
assert.equal(runtime.QUERY_VERSION,'ms_messages_delta_from_now_v2');
const env={LUNA_DEPLOYMENT:'sunset-staging',DEFAULT_CLIENT_SLUG:'sunset',LUNA_EMAIL_DELTA_RUNTIME_COMPOSITION_ENABLED:'true',LUNA_EMAIL_DELTA_WORKER_ENABLED:'true',LUNA_EMAIL_DELTA_ADMIN_ENABLED:'false',LUNA_AUTO_SEND_ENABLED:'false',LUNA_EMAIL_OAUTH_CLIENT_ID:'11111111-1111-4111-8111-111111111111',EMAIL_GRANT_ENVELOPE_AZURE_KV_COMPOSITION_ENABLED:'true',EMAIL_GRANT_ENVELOPE_AZURE_KV_RUNTIME_ACTIVATION_ENABLED:'true',EMAIL_GRANT_ENVELOPE_AZURE_KV_TRUSTED_HOST:'luna-sunset-staging-kv.vault.azure.net',EMAIL_GRANT_ENVELOPE_AZURE_KV_VERSIONED_KEY_ID:'https://luna-sunset-staging-kv.vault.azure.net/keys/luna-email-grant-kek/fde9704bd37b45fabe1f12a6a615b032'};
assert.equal(runtime.resolveEmailDeltaSunsetStagingRuntimeReadiness(env).runtime_activation,true);
assert.equal(runtime.resolveEmailDeltaSunsetStagingRuntimeReadiness({...env,LUNA_AUTO_SEND_ENABLED:'true'}).ok,false);
assert.equal(runtime.resolveEmailDeltaSunsetStagingRuntimeReadiness({...env,LUNA_DEPLOYMENT:'production'}).ok,false);
const migration=fs.readFileSync(path.join(root,'database/migrations/080_tenant_email_delta_from_now_v2.sql'),'utf8');
assert.match(migration,/ms_messages_delta_from_now_v2/); assert.match(migration,/refuses existing delta state/);
assert.match(migration,/FOREIGN KEY \(client_id, endpoint_id, location_id\)[\s\S]*REFERENCES tenant_channel_endpoints\(client_id, id, location_id\)/);
const staff=fs.readFileSync(path.join(root,'scripts/staff-query-api.js'),'utf8');
const createAt=staff.lastIndexOf('createEmailDeltaSunsetStagingRuntimeComposition({');
const startAt=staff.indexOf('await EMAIL_DELTA_RUNTIME.start()',createAt);
const listenAt=staff.indexOf('server.listen(',startAt);
assert.ok(createAt>0&&startAt>createAt&&listenAt>startAt,'runtime must create/start before listen');
assert.match(staff,/catch \{[\s\S]{0,100}process\.exitCode = 1;[\s\S]{0,100}return;[\s\S]{0,100}server\.listen/);
assert.match(staff,/ON_SHUTDOWN_BEGIN_HOOK[\s\S]*EMAIL_DELTA_RUNTIME\.stop/);
console.log('PASS EMAIL-M1-020 from-now, migration, activation, lifecycle readiness');
