'use strict';
/** Exact compatibility facade; authority is lexically private in the lifecycle umbrella. */
module.exports = require('./email-microsoft-reauthorization-lifecycle').phaseBReauthorizationTransactionService;
