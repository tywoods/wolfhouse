'use strict';

// Deliberately no authority surface. Start authority is created inside each
// production integration composition and never enters the module registry.
module.exports = Object.freeze({});
