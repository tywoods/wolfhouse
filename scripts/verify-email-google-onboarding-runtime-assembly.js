'use strict';
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const owner = require('./lib/email-google-onboarding-runtime-assembly');
const { createGoogleOnboardingRuntimeAssembly } = owner;
const freeze = Object.freeze;
const CLIENT='aaaaaaaa-bbbb-4ccc-8ddd-eeeeeeeeeeee';
const LOCATION='sunset-somo';
const ENDPOINT='11111111-2222-4333-8444-555555555555';
const APP='9876543210-sunset.apps.googleusercontent.com';
const REDIRECT='https://staff-staging.lunafrontdesk.com/staff/email/google/callback';
const REF='secret-ref:email/google/sunset-staging-oauth-client';
const config=(onboardingEnabled=false,patch={})=>freeze({tenantSlug:'sunset',clientId:CLIENT,locationId:LOCATION,endpointId:ENDPOINT,applicationClientId:APP,redirectUri:REDIRECT,secretRef:REF,onboardingEnabled,...patch});
function harness(){const calls={random:0,sha:0,now:0,create:0,consume:0,https:0,crypto:0,timers:0,envelope:0,install:0,secret:0};
 const cryptography=freeze({randomUUID(){calls.random++;throw Error('leak')},randomBytes(){calls.random++;throw Error('leak')},sha256Ascii(){calls.sha++;throw Error('leak')}});
 const clock=freeze({now(){calls.now++;throw Error('leak')},nowEpochSeconds(){calls.now++;throw Error('leak')}});
 const repository=freeze({create(){calls.create++;throw Error('leak')},consume(){calls.consume++;throw Error('leak')}});
 const https=freeze({request(){calls.https++;throw Error('leak')}});
 const crypto=freeze({createPublicKey(){calls.crypto++;throw Error('leak')},verify(){calls.crypto++;throw Error('leak')}});
 const timers=freeze({setTimeout(){calls.timers++;throw Error('leak')},clearTimeout(){calls.timers++;throw Error('leak')}});
 const envelopeProvider=freeze({sealGrantPayload(){calls.envelope++;throw Error('leak')},openGrantPayload(){calls.envelope++;throw Error('leak')},rewrapGrantDek(){calls.envelope++;throw Error('leak')}});
 const installer=freeze({installVerifiedGrant(){calls.install++;throw Error('leak')}});
 const secretProvider=freeze({resolveClientSecret(){calls.secret++;throw Error('leak')}});
 const signatureVerifier=freeze({verifySignature(){calls.crypto++;throw Error('leak')}});
 return {calls,dependencies:freeze({cryptography,clock,repository,https,crypto,timers,envelopeProvider,installer,secretProvider,signatureVerifier})};}
const zero=c=>assert.deepEqual(c,{random:0,sha:0,now:0,create:0,consume:0,https:0,crypto:0,timers:0,envelope:0,install:0,secret:0});
const tests=[];const test=(n,f)=>tests.push([n,f]);
test('exports only frozen factory and exact frozen narrow default-off surface',()=>{const h=harness(),r=createGoogleOnboardingRuntimeAssembly(config(),h.dependencies);assert.deepEqual(Reflect.ownKeys(owner),['createGoogleOnboardingRuntimeAssembly']);assert(Object.isFrozen(owner));assert.deepEqual(Reflect.ownKeys(r),['configuration','startOnboarding','completeOnboardingCallback','deriveMailboxAuthority']);assert(Object.isFrozen(r));assert(Object.isFrozen(r.configuration));assert.deepEqual(r.configuration,config());zero(h.calls);});
test('construction performs zero SQL network crypto timer secret envelope installer route env or provider work',async()=>{const h=harness();createGoogleOnboardingRuntimeAssembly(config(),h.dependencies);await Promise.resolve();zero(h.calls);});
test('creates independent runtime instances while pinning exact frozen dependency owners',()=>{const h=harness();const a=createGoogleOnboardingRuntimeAssembly(config(),h.dependencies),b=createGoogleOnboardingRuntimeAssembly(config(),h.dependencies);assert.notStrictEqual(a,b);assert.notStrictEqual(a.configuration,b.configuration);zero(h.calls);});
test('rejects mutable inherited excess reordered accessor and proxy configuration without effects',()=>{for(const bad of [{...config()},freeze({...config(),x:1}),freeze({clientId:CLIENT,tenantSlug:'sunset',locationId:LOCATION,endpointId:ENDPOINT,applicationClientId:APP,redirectUri:REDIRECT,secretRef:REF,startEnabled:false,callbackEnabled:false,authorityEnabled:false}),freeze(Object.assign(Object.create(null),config())),new Proxy(config(),{})]){const h=harness();assert.throws(()=>createGoogleOnboardingRuntimeAssembly(bad,h.dependencies));zero(h.calls)}const x={...config()};Object.defineProperty(x,'tenantSlug',{enumerable:true,get(){throw Error('leak')}});Object.freeze(x);assert.throws(()=>createGoogleOnboardingRuntimeAssembly(x,harness().dependencies));});
test('accepts one canonical assembly activation decision and rejects split or non-boolean activation',()=>{for(const enabled of [false,true]){const h=harness();createGoogleOnboardingRuntimeAssembly(config(enabled),h.dependencies);zero(h.calls)}for(const bad of [null,0,'true'])assert.throws(()=>createGoogleOnboardingRuntimeAssembly(config(bad),harness().dependencies));for(const patch of [{startEnabled:true},{callbackEnabled:true},{authorityEnabled:true},{tenantSlug:'wolfhouse'},{locationId:'sunset-other'},{endpointId:'22222222-2222-4333-8444-555555555555'},{applicationClientId:'other.apps.googleusercontent.com'},{redirectUri:'https://evil.test/callback'},{secretRef:'secret-ref:other'}])assert.throws(()=>createGoogleOnboardingRuntimeAssembly(config(false,patch),harness().dependencies));});
test('rejects mutable proxy accessor inherited excess and reordered dependency/capability bags',()=>{const h=harness(),d=h.dependencies;for(const bad of [{...d},freeze({...d,x:1}),freeze({clock:d.clock,cryptography:d.cryptography,repository:d.repository,https:d.https,crypto:d.crypto,timers:d.timers,envelopeProvider:d.envelopeProvider,installer:d.installer,secretProvider:d.secretProvider,signatureVerifier:d.signatureVerifier}),new Proxy(d,{})])assert.throws(()=>createGoogleOnboardingRuntimeAssembly(config(),bad));assert.throws(()=>createGoogleOnboardingRuntimeAssembly(config(),freeze({...d,https:freeze({request(){},send(){}})})));zero(h.calls);});
test('default-off operations fail sanitized before every capability and provider input cannot select authority',async()=>{const h=harness(),r=createGoogleOnboardingRuntimeAssembly(config(),h.dependencies);for(const [fn,input] of [[r.startOnboarding,freeze({tenantSlug:'sunset',clientId:CLIENT,locationId:LOCATION,endpointId:ENDPOINT,staffUserId:CLIENT,authSessionId:CLIENT})],[r.completeOnboardingCallback,freeze({tenantSlug:'sunset',clientId:CLIENT,locationId:LOCATION,endpointId:ENDPOINT,authSessionId:CLIENT,query:'x'})],[r.deriveMailboxAuthority,freeze({tenantSlug:'sunset',clientId:CLIENT,locationId:LOCATION,endpointId:ENDPOINT,expectedNonce:'n',idToken:'x',accessToken:'x',nowEpochSeconds:1,provider:'other'})]])await assert.rejects(Promise.resolve().then(()=>fn(input)),e=>e.code==='GOOGLE_ONBOARDING_RUNTIME_DISABLED'&&!JSON.stringify(e).includes('secret'));zero(h.calls);});
test('disabled failures are fresh sanitized stackless values',()=>{const r=createGoogleOnboardingRuntimeAssembly(config(),harness().dependencies);let a,b;try{r.startOnboarding(null)}catch(e){a=e}try{r.startOnboarding(null)}catch(e){b=e}assert.notStrictEqual(a,b);for(const e of [a,b]){assert.equal(e.code,'GOOGLE_ONBOARDING_RUNTIME_DISABLED');assert.equal(e.stack,undefined);assert.equal(JSON.stringify(e).includes('/'),false)}});
test('enabled start delegates to authentic start owner with the assembly binding',async()=>{const h=harness(),bytes=[Buffer.alloc(32,1),Buffer.alloc(32,2),Buffer.alloc(32,3)];const calls=[];const cryptography=freeze({randomUUID(){calls.push('uuid');return CLIENT},randomBytes(n){calls.push(['bytes',n]);return bytes.shift()},sha256Ascii(v){calls.push(['sha',v]);return require('node:crypto').createHash('sha256').update(v,'ascii').digest()}});const clock=freeze({now(){calls.push('now');return '2026-08-12T00:00:00.000Z'},nowEpochSeconds(){return 0}});const repository=freeze({create(dto){calls.push(['create',dto]);return freeze({operationId:dto.operationId,expiresAt:dto.expiresAt})},consume(){throw Error('unused')}});const dependencies=freeze({...h.dependencies,cryptography,clock,repository});const runtime=createGoogleOnboardingRuntimeAssembly(config(true),dependencies);const out=await runtime.startOnboarding(freeze({tenantSlug:'sunset',clientId:CLIENT,locationId:LOCATION,endpointId:ENDPOINT,staffUserId:CLIENT,authSessionId:CLIENT}));assert.equal(out.expiresAt,'2026-08-12T00:10:00.000Z');assert.match(out.authorizationUrl,/accounts\.google\.com/);assert.equal(calls.filter(x=>Array.isArray(x)&&x[0]==='create').length,1);});
test('surface exposes no model send write query secret token installer or generic capability',()=>{const r=createGoogleOnboardingRuntimeAssembly(config(),harness().dependencies);const text=Reflect.ownKeys(r).join(' ');for(const word of ['model','send','write','query','secret','token','installer','request'])assert.equal(text.toLowerCase().includes(word),false);});
test('source has exact owner imports and no routes startup environment activation SDK or direct capability calls',()=>{const source=fs.readFileSync(path.join(__dirname,'lib/email-google-onboarding-runtime-assembly.js'),'utf8');const imports=[...source.matchAll(/require\(\s*['"]([^'"]+)['"]\s*\)/g)].map(x=>x[1]);assert.deepEqual(imports,['node:util','./email-google-oauth-start','./email-google-oauth-callback-consume','./email-google-transaction-completion-factory','./email-google-oauth-callback-completion','./email-google-mailbox-authority-composition']);for(const re of [/process\.env/,/staff-query-api|express|router|listen\s*\(/i,/googleapis|@google/i,/\.query\s*\(/,/\.request\s*\(/,/resolveClientSecret\s*\(/,/installVerifiedGrant\s*\(/])assert.equal(re.test(source),false,String(re));});
test('package exposes only focused verifier script addition',()=>{const p=require('../package.json');assert.equal(p.scripts['verify:email-google-onboarding-runtime-assembly'],'node scripts/verify-email-google-onboarding-runtime-assembly.js');});
(async()=>{for(const[n,f]of tests){await f();process.stdout.write(`ok - ${n}\n`)}process.stdout.write(`PASS verify:email-google-onboarding-runtime-assembly (${tests.length} named offline tests)\n`)})().catch(e=>{process.stderr.write(`${e.stack||e}\n`);process.exitCode=1});
