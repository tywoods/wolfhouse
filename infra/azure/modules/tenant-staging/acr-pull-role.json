{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "description": "DISABLED — historical residual only. Stage 2D2 no longer nested-deploys AcrPull from tenant or shared RG. JS apply owns exact deterministic AcrPull via ARM roleAssignments PUT at the existing shared ACR scope after infra identity exists (temp ACR RBAC Admin). Do not re-enable this module: tenant-RG nested deploy ResourceNotFound on whstagingacr; shared-RG nested deploy needs deployments/write on wh-staging-rg."
  },
  "parameters": {
    "acrName": {
      "type": "string"
    },
    "acrResourceGroupName": {
      "type": "string"
    },
    "principalId": {
      "type": "string"
    }
  },
  "resources": []
}
