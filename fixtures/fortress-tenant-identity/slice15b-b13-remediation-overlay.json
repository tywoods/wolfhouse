{
  "schema_version": 1,
  "slice": "FORTRESS-15B",
  "boundary_id": "B13_stripe_webhook_payment_lookup",
  "status": "remediated",
  "historical_audit_unchanged": true,
  "historical_artifacts": [
    "fixtures/fortress-tenant-identity/boundary-matrix.json",
    "fixtures/fortress-tenant-identity/attack-cases.json",
    "docs/FORTRESS-TENANT-IDENTITY-BOUNDARY-MATRIX.md"
  ],
  "note": "15A matrix/doc retain pre-fix vulnerable verdict for B13 as historical audit. This overlay records the 15B remediation without rewriting 15A artifacts.",
  "remediation": {
    "outcome_id": "15B_stripe_metadata_payment_lookup_tenant_bind",
    "owner_files": [
      "scripts/lib/stripe-webhook-payment-truth.js",
      "scripts/lib/stripe-webhook-tenant-config.js",
      "scripts/lib/stripe-payment-reconcile.js",
      "scripts/staff-query-api.js"
    ],
    "controls": [
      "expectedClientSlug required on lookup and validate",
      "session-id and metadata.payment_id SELECTs scoped by cl.slug = $2",
      "metadata fallback requires metadata.client_slug === expectedClientSlug before metadata.payment_id UUID query (rejected fallback does not query that object / cannot leak its existence; session-id SELECT may already have run)",
      "independent pm.client_slug === expectedClientSlug validation",
      "runtime tenant from STRIPE_WEBHOOK_CLIENT_SLUG (compat DEFAULT_CLIENT_SLUG); conflict/missing fail closed"
    ]
  },
  "master_basis_15a": "8ed81111b9a67a656dee0b7dbd5a46ab91ca125c",
  "live_mutation": false
}
