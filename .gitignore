node_modules/
infra/.env
.env
.env.local
hermes-local/.env
hermes-vm-env/
.wh-sync.bundle
*.log
docs/api keys.txt
api keys.txt
__pycache__/
reports/*.csv
reports/*.json
logs/*.jsonl

# Client secrets (real phone numbers, passwords) — never commit; keep .example versions
config/clients/*.secrets.json

# Staging channel routing with real Meta phone_number_id values — copy from channel-routing.staging.example.json outside git
config/clients/channel-routing.staging.json

# Private / raw WhatsApp exports (never commit)
data/private/
data/raw-whatsapp/
data/whatsapp-raw/
data/whatsapp-redacted/
docs/knowledge/whatsapp-raw/
docs/knowledge/whatsapp-private/
*WhatsApp Chat*.txt
*.wa-export.txt
*_chat.txt
*_redaction_map.json
*_pii_map.json

# generated nightly run artifacts
reports/luna-nightly/

# debrief draft fixtures (review before adding to the suite)
reports/luna-debrief/

# OpenClaw agent-workspace artifacts that leak into the WH repo root
# (the real Luna SOUL lives at docker/hermes-staging/SOUL.md — these root copies are strays)
/SOUL.md
/IDENTITY.md
/USER.md
/TOOLS.md
/HEARTBEAT.md
/AGENTS.md
/MEMORY.md
/OPS-LUNA.md
/.openclaw/
/.openclaw-cli-images/
/memory/
