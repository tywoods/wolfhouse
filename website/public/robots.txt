# Luna Front Desk — robots.txt
# Crawling is permitted; indexing of preview deployments is gated separately
# via a <meta name="robots" content="noindex, nofollow"> tag injected at build
# time when PUBLIC_INDEXABLE is not "true". Production builds remove that tag
# and allow full indexing.

User-agent: *
Allow: /

Sitemap: /sitemap-index.xml
